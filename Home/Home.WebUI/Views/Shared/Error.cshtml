@model IEnumerable<string>
@{
    ViewBag.Title = "Ошибка";
    Layout = "~/Views/Shared/_RegisterForm.cshtml";
}
<div class="alert alert-danger">
    @switch (Model.Count())
    {
        case 0:
            @: Что-то пошло не так. Пожалуйста, попробуйте еще раз!
            break;
        case 1:
            @Model.First();
            break;
        default:
            @: Следующие ошибки были обнаружены:
            <ul>
                @foreach (string error in Model)
                {
                    <li>@error</li>
                }
            </ul>
            break;
    }
</div>
@Html.ActionLink("Ок", "List", "General", null, new { @class = "btn btn-default" })

@section B{
}

@section C{
}
